#ifndef _KMEANS_
#define _KMEANS_

/* Retorna o resultado da funcao para um conjunto de pontos numa determinada combinacao */
double kmeans(double **pontos, int *comb, int k, int qtd_p, int dim_p);

#endif
